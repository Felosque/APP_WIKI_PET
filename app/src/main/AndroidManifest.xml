<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wikipets">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ActividadesEstructural">
        <activity android:name="com.example.wikipets.GUIListar"></activity>
        <activity android:name="com.example.wikipets.GUIBuscar" />
        <activity android:name="com.example.wikipets.GUIEditar" />
        <activity android:name="com.example.wikipets.GUIEliminar" />
        <activity android:name="com.example.wikipets.GUIAdicionar" />
        <activity android:name="com.example.wikipets.GUIMenu" />
        <activity android:name="com.example.wikipets.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>